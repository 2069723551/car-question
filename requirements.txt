# 汽车销量预测系统 全量依赖
# 安装：pip install -r requirements.txt
# 包含：训练脚本（src/）+ 网页后端（car-sales-backend）所需依赖

# ---- 数据与训练 ----
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# ---- 网页后端（FastAPI + SQLAlchemy）----
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
